# next-static-page

A tiny Next.js project that builds to a static export (`output: 'export'`).

## Development

```bash
npm run dev
```

## Tests

```bash
npm test
```

## Build (static export)

```bash
npm run build
```

The exported site is generated into `out/`.
